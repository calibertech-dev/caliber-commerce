<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
  <fullName>Next_Retry_Date__c</fullName>
  <label>Next Retry Date</label>
  <type>Date</type>
  <formula>
          IF(
          ISPICKVAL(Status__c, "Failed"),
          CASE(
          Retry_Count__c,
          0, TODAY() + 1,
          1, TODAY() + 3,
          2, TODAY() + 7,
          TODAY() + 7
          ),
          NULL
          )
  </formula>
</CustomField>
